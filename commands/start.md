---
description: "Start Auto Enter for Claude Code permission prompts on Windows. Optional argument: interval seconds, default 5."
argument-hint: "[interval-seconds]"
disable-model-invocation: true
---

Invoke the auto-enter:auto-enter skill and follow it exactly.

Use the user-provided interval if present. If no interval is provided, use `5` seconds.

After starting, remind the user that they can stop it with `/auto-enter:stop`.
